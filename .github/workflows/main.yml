name: Build and Deploy
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: echo helloo world asdads

      - name: Deploying using ssh
        uses: tavethiyaparul/ssh-action@main
        with:
          host: 167.86.109.223
          username: root
          password: 77NHg9e5YT9Jpmsc3
          port: 22
          script: |
            cd /var/www/public_html/demo_project/Todo/
            sudo rm -rf node_modules
            sudo git pull
            sudo npm install
            pm2 list
            pm2 restart 1